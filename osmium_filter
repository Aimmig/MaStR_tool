# xxx to be replaced with the downloaded state date
# keep node and ways with generator because some wind turbines
# are only mapped as generator without source or method and
# some are mapped as ways (for whatever reason??)
osmium tags-filter xxx.osm.pbf nw/power=generator -o xxx_wka_osm.pbf
